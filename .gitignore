# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Dependencies (if using npm for build tools later)
node_modules/

# Environment
.env

# Case study screenshots. They get imported to the media library and
# referenced from content.html by URL, so the repo does not need a second
# copy - no other case study directory carries one.
case-studies/**/*.jpg
case-studies/**/*.png
